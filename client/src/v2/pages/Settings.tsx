import React from 'react';

const Settings: React.FC = () => {
  return (
    <div style={{ padding: 24 }}>
      <h2>V2 Settings</h2>
      <p>Manage API keys and preferences. Uses unified settings APIs.</p>
    </div>
  );
};

export default Settings;
